
let initialState = {
    noveltyVis: true,
};

const noveltyR = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default noveltyR;