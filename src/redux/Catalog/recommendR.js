import Img from '../../assets/img/gul.jpg'

let initialState = {
    reccomendList: {
        list: [
            {
                // 
            },
        ],
    },
    continueViewVis: true,
};

const recommendR = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default recommendR;