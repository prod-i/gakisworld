
let initialState = {
    sliderVis: true,
};

const sliderR = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default sliderR;