let initialState = {
    bannerVis: true,
};

const bannerR = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default bannerR;